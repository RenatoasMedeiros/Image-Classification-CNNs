Testado usando python 3.8 (anaconda).

Required packages:
pip install pillow flask tensorflow

Colocar um dos modelos fornecidos na root desta (ao lado do app.py) pasta e mudar o nome para 'model.keras'